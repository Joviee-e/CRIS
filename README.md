# Western Railway — ID Verification Backend

A Flask-based backend providing endpoints for user registration, authentication (JWT), application submission, verification and approval.

## Quick start

### Prerequisites
- Python 3.10+ recommended
- Docker (optional)
- Oracle Instant Client (only if connecting to Oracle locally)

### Setup locally
1. Clone repo and enter backend directory:
   ```bash
   git clone <repo-url>
   cd CRIS

